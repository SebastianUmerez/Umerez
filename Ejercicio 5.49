#include <stdio.h>

int main() {
    int n, i;

    // Pedir el número
    printf("Ingrese un numero: ");
    scanf("%d", &n);

    // Calcular e imprimir la tabla de multiplicar
    for (i = 1; i <= 10; i++) {
        printf("%d * %d = %d\n", i, n, i * n);
    }

    return 0;
}
