#include <stdio.h>

int main() {
    int i;

    // Bajada: de 5 a 0
    for (i = 5; i >= 0; i--) {
        printf("%d ", i);
    }

    // Subida: de 1 a 5
    for (i = 1; i <= 5; i++) {
        printf("%d ", i);
    }

    printf("\n");
    return 0;
}
