#include <stdio.h>
#define N 4
int main() {
    int i;
    int a[N];
    int b[N];
    int suma;
    for(i = 0; i<N; i++){
        printf("ingrese el elemento[%d]: ", i);
        scanf("%d", &a[i]);
    }
    printf("los elementos son: \n");
    for(i=0;i<N;i++){
        if(i==0) b[i] = a[i];
        else b[i] = a[i] + b[i-1];
        printf("b[%d]: %d a: %d\n",i,b[i],a[i]);
    }
    return 0;
}
